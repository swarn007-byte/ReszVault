# Backend base URL (no trailing slash).
# Local: http://localhost:3000
# Production frontend URL: https://reszvault.vercel.app
# Production backend URL: https://reszvault.onrender.com
VITE_API_BASE_URL=http://localhost:3000

# Auth is handled by the backend through Better Auth.
# Google credentials belong in backend environment variables:
# GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
